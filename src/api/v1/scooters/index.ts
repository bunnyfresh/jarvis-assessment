// import express from 'express'

// import {
//   deleteScooter,
//   getScooters,
//   getScooter,
//   postScooter,
//   getScooterLocation,
// } from './controller'

// const router = express.Router()

// Sliding session - also used to refresh jwt payload (such as role change)
// router.get('/', getScooters)
// router.get('/:id', getScooter)
// router.get('/:id/location', getScooterLocation)
// router.post('/', postScooter)
// router.delete('/:id', deleteScooter)

// export const scooterRouter = router
